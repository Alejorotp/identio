{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": [
        "cd mvp && chmod +x ./mvnw && ./mvnw clean package -DskipTests"
      ]
    }
  },
  "deploy": {
    "startCommand": "java -jar mvp/target/mvp-0.0.1-SNAPSHOT.jar"
  }
}